# DEEPDELL

One-page DEEPDELL brand website built as a lightweight static site for GitHub Pages.

## Deployment

Enable **Settings → Pages → Deploy from a branch → main → /(root)** in the repository settings.

The site entry point is `index.html`.