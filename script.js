(()=>{const body=document.body,loader=document.getElementById('site-loader'),progress=document.querySelector('.scroll-progress'),header=document.querySelector('.site-header'),menu=document.querySelector('.menu'),themeToggle=document.querySelector('.theme-toggle');const reduce=window.matchMedia('(prefers-reduced-motion: reduce)').matches,coarse=window.matchMedia('(pointer: coarse)').matches;const saved=localStorage.getItem('deepdell-theme');if(saved==='light'||(!saved&&window.matchMedia('(prefers-color-scheme: light)').matches))body.classList.add('light');const syncTheme=()=>{const light=body.classList.contains('light');themeToggle?.setAttribute('aria-label',light?'Switch to dark theme':'Switch to light theme');themeToggle?.setAttribute('aria-pressed',String(light));const meta=document.querySelector('meta[name="theme-color"]');if(meta)meta.setAttribute('content',light?'#f5faff':'#011434');};syncTheme();themeToggle?.addEventListener('click',()=>{body.classList.toggle('light');localStorage.setItem('deepdell-theme',body.classList.contains('light')?'light':'dark');syncTheme();});const finishLoader=()=>{if(!loader)return;loader.classList.add('loader-done');setTimeout(()=>loader.remove(),520);};if(document.readyState==='complete')setTimeout(finishLoader,180);else window.addEventListener('load',()=>setTimeout(finishLoader,220),{once:true});menu?.addEventListener('click',()=>{const open=header.classList.toggle('menu-open');menu.setAttribute('aria-expanded',String(open));});document.querySelectorAll('.nav a').forEach(a=>a.addEventListener('click',()=>header?.classList.remove('menu-open')));const onScroll=()=>{const y=window.scrollY,max=document.documentElement.scrollHeight-window.innerHeight;if(progress)progress.style.width=`${max>0?Math.min(100,y/max*100):0}%`;header?.classList.toggle('scrolled',y>20);};window.addEventListener('scroll',onScroll,{passive:true});onScroll();const targets=document.querySelectorAll('.service,.ai-grid article,.why-grid article,.steps article,.reveal,.arch-card,.vertical,.engagement,.digital-card,.protocol,.agent-step,.ops-copy,.live-console,.page-card');if('IntersectionObserver'in window&&!reduce){const observer=new IntersectionObserver(entries=>entries.forEach(e=>{if(e.isIntersecting){e.target.classList.add('visible');observer.unobserve(e.target);}}),{threshold:.08,rootMargin:'0px 0px -30px'});targets.forEach(el=>{el.classList.add('reveal-item');observer.observe(el);});}else targets.forEach(el=>el.classList.add('visible'));const art=document.querySelector('.hero-art');if(art&&!reduce&&!coarse){art.addEventListener('pointermove',e=>{const r=art.getBoundingClientRect(),x=(e.clientX-r.left)/r.width-.5,y=(e.clientY-r.top)/r.height-.5;art.style.setProperty('--mx',`${x*16}px`);art.style.setProperty('--my',`${y*12}px`);});art.addEventListener('pointerleave',()=>{art.style.setProperty('--mx','0px');art.style.setProperty('--my','0px');});}if(!reduce&&!coarse){document.querySelectorAll('.btn,.theme-toggle').forEach(el=>{el.addEventListener('pointermove',e=>{const r=el.getBoundingClientRect(),x=(e.clientX-r.left)/r.width-.5,y=(e.clientY-r.top)/r.height-.5;el.style.setProperty('--hx',`${x*3}px`);el.style.setProperty('--hy',`${y*3}px`);});el.addEventListener('pointerleave',()=>{el.style.setProperty('--hx','0px');el.style.setProperty('--hy','0px');});});}const page=location.pathname.split('/').pop()||'index.html';document.querySelectorAll('.nav a').forEach(a=>{if(a.getAttribute('href')===page||(page===''&&a.getAttribute('href')==='index.html'))a.setAttribute('aria-current','page');});})();