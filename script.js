(()=>{const body=document.body,loader=document.getElementById('site-loader'),progress=document.querySelector('.scroll-progress'),header=document.querySelector('.site-header'),menu=document.querySelector('.menu');
/* DEEPDELL is intentionally dark-only. Remove any legacy theme state/control. */
body.classList.remove('light');localStorage.removeItem('deepdell-theme');document.querySelectorAll('.theme-toggle').forEach(el=>el.remove());const meta=document.querySelector('meta[name="theme-color"]');if(meta)meta.setAttribute('content','#011434');
/* Use the supplied orbital icon inside the DEEPDELL lockup. */
document.querySelectorAll('.brand img,.footer-brand img').forEach(img=>{img.src='assets/deepdell-lockup.svg';img.alt='DEEPDELL';});
/* Technical preloader */
const finishLoader=()=>{if(!loader)return;loader.classList.add('loader-done');setTimeout(()=>loader.remove(),500)};if(document.readyState==='complete')setTimeout(finishLoader,180);else window.addEventListener('load',()=>setTimeout(finishLoader,220),{once:true});
/* Mobile navigation */
menu?.addEventListener('click',()=>{const open=header.classList.toggle('menu-open');menu.setAttribute('aria-expanded',String(open));});document.querySelectorAll('.nav a').forEach(a=>a.addEventListener('click',()=>header?.classList.remove('menu-open')));
/* Scroll progress + 3D scene movement */
let ticking=false;const onScroll=()=>{const y=window.scrollY,max=document.documentElement.scrollHeight-window.innerHeight;if(progress)progress.style.width=`${max>0?Math.min(100,y/max*100):0}%`;header?.classList.toggle('scrolled',y>20);const art=document.querySelector('.hero-art');if(art&&window.matchMedia('(prefers-reduced-motion: no-preference)').matches){const depth=Math.max(-34,Math.min(18,y*.035));art.style.setProperty('--scroll-y',`${depth}px`);art.style.setProperty('--ry',`${Math.max(-3,Math.min(3,y*.006))}deg`);}ticking=false};window.addEventListener('scroll',()=>{if(!ticking){ticking=true;requestAnimationFrame(onScroll)}},{passive:true});onScroll();
/* Scroll reveal */
const reduce=window.matchMedia('(prefers-reduced-motion: reduce)').matches;const targets=document.querySelectorAll('.service,.ai-grid article,.why-grid article,.steps article,.reveal,.arch-card,.vertical,.engagement,.digital-card,.protocol,.agent-step,.ops-copy,.live-console,.page-card');if('IntersectionObserver'in window&&!reduce){const observer=new IntersectionObserver(entries=>entries.forEach(e=>{if(e.isIntersecting){e.target.classList.add('visible');observer.unobserve(e.target)}}),{threshold:.08,rootMargin:'0px 0px -30px'});targets.forEach(el=>{el.classList.add('reveal-item');observer.observe(el)});}else targets.forEach(el=>el.classList.add('visible'));
/* Game-like hero: orbital parallax, cursor targeting and reactive nodes */
const art=document.querySelector('.hero-art');const coarse=window.matchMedia('(pointer: coarse)').matches;if(art&&!reduce&&!coarse){const dots=[...art.querySelectorAll('.dot')];art.addEventListener('pointerenter',()=>art.classList.add('is-hovering'));art.addEventListener('pointermove',e=>{const r=art.getBoundingClientRect(),x=(e.clientX-r.left)/r.width-.5,y=(e.clientY-r.top)/r.height-.5;art.style.setProperty('--mx',`${x*16}px`);art.style.setProperty('--my',`${y*12}px`);art.style.setProperty('--rx',`${-y*4}deg`);art.style.setProperty('--ry',`${x*5}deg`);art.style.setProperty('--cursor-x',`${(x+.5)*100}%`);art.style.setProperty('--cursor-y',`${(y+.5)*100}%`);dots.forEach((dot,i)=>{const dx=((i*29)%100)/100-.5,dy=((i*47)%100)/100-.5,dist=Math.hypot(x-dx,y-dy);const push=Math.max(0,1-dist*2.8);dot.style.transform=`translate(${(x-dx)*-18*push}px,${(y-dy)*-18*push}px) scale(${1+push*.35})`;dot.style.boxShadow=push?`0 0 ${14+push*18}px rgba(32,243,251,.75)`:''})});art.addEventListener('pointerleave',()=>{art.classList.remove('is-hovering');art.style.setProperty('--mx','0px');art.style.setProperty('--my','0px');art.style.setProperty('--rx','0deg');art.style.setProperty('--ry','0deg');dots.forEach(d=>{d.style.transform='';d.style.boxShadow=''})});art.addEventListener('pointerdown',()=>{art.classList.remove('hit');void art.offsetWidth;art.classList.add('hit')});}
/* 3D card hover / light-game surface */
if(!reduce&&!coarse){document.querySelectorAll('.service,.page-card,.ai-grid article,.why-grid article,.steps article,.arch-card,.engagement,.digital-card,.protocol,.live-console').forEach(card=>{card.addEventListener('pointermove',e=>{const r=card.getBoundingClientRect(),x=(e.clientX-r.left)/r.width-.5,y=(e.clientY-r.top)/r.height-.5;card.style.setProperty('--card-rx',`${-y*5}deg`);card.style.setProperty('--card-ry',`${x*6}deg`);card.style.setProperty('--spot-x',`${(x+.5)*100}%`);card.style.setProperty('--spot-y',`${(y+.5)*100}%`)});card.addEventListener('pointerleave',()=>{card.style.setProperty('--card-rx','0deg');card.style.setProperty('--card-ry','0deg')})});document.querySelectorAll('.btn').forEach(el=>{el.addEventListener('pointermove',e=>{const r=el.getBoundingClientRect(),x=(e.clientX-r.left)/r.width-.5,y=(e.clientY-r.top)/r.height-.5;el.style.setProperty('--bx',`${x*3}px`);el.style.setProperty('--by',`${y*3}px`);el.style.transform='translate(var(--bx),var(--by)) translateY(-2px)')});el.addEventListener('pointerleave',()=>{el.style.transform='';el.style.removeProperty('--bx');el.style.removeProperty('--by')})})}
/* Active navigation */
const page=location.pathname.split('/').pop()||'index.html';document.querySelectorAll('.nav a').forEach(a=>{if(a.getAttribute('href')===page||(page===''&&a.getAttribute('href')==='index.html'))a.setAttribute('aria-current','page')});})();